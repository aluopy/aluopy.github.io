# .travis.yml
# This file should be at the root of your project
# Without a credit card, the free version cannot be used
language: ruby
cache: bundler
gemfile: Gemfile
script:
  - bundle exec jekyll algolia
branches:
  only:
    # Change this to gh-pages if you're deploying using the gh-pages branch
    - master
rvm:
 - 2.4
